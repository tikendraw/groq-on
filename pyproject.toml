[tool.poetry]
name = "groqon"
version = "0.1.1"
description = "Use Groq.com as llm"
authors = ["tikendraw <tikendraksahu1029@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
playwright = "^1.43.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
groqon = 'groqon.groq:main'
groq = 'groqon.groq:main'
